# ---------- 依赖与构建产物（容器内会重新安装 / 构建） ----------
**/node_modules
**/dist
**/.output
**/.nuxt
**/.data
**/*.tsbuildinfo

# ---------- 运行时数据（绝不进镜像） ----------
glimmer-data
data
uploads
*.db
*.db-journal
*.db-wal
*.db-shm

# ---------- 本地配置与密钥 ----------
.env
.env.*
!.env.example

# ---------- 证书 / 版本控制 / 本地工具 ----------
certs
.git
.github
.gitignore
.gitattributes
.workbuddy
.logs
*.log

# ---------- 构建期临时文件 ----------
tsup.config.bundled_*.mjs

# ---------- 仅本地留存、不对外公开的文档 ----------
# Dockerfile 目前只 COPY 源码目录，理论上带不到镜像里；
# 这里显式排除，一是缩小构建上下文，二是防止将来 Dockerfile 改动
# （比如误用 COPY . .）把它们打进镜像。
提示词.md
NOTES.md
